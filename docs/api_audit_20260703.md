# 后端API审查报告 2026-07-03 23:50

## Phase 0: Issue队列
- ✅ 待修队列为空

## Phase 1: 全端点测试结果

### 测试概况
- **GET端点测试**: 83个端点
- **通过**: 64 (77%)
- **慢响应(>3s)**: 3
- **失败**: 19 → 其中16个为预期404(路由未注册)

### 预期404(路由已删除/未注册)
| 端点 | 原因 |
|------|------|
| `/market/*` (15个) | market_router已从前端移除，路由未注册 |
| `/report`, `/report/dates` | report_router未注册 |

### 需关注的问题
| 问题 | 详情 | 严重度 |
|------|------|--------|
| `/scanner/strategy/performance-history` 404 | 前端用`/scanner/strategy/performance-history`，后端路径是`/scanner/performance-history` | ⚠️ 中 |
| `/stocks/search` 422 | 需要参数`keyword`而非`q`，前端需确认 | ✅ 已正确 |
| `/factor/view` 422 | 需要参数`ts_code`，正确行为 | ✅ 已正确 |
| `/backtest/history` 401 | 需认证，正确行为 | ✅ 已正确 |
| `/system/auto-fill-detect` 4.2s | MongoDB查询较慢，可优化 | ⚡ 低 |
| `/factor/batch-view` 3.5s | 批量因子查询，可接受 | ⚡ 低 |
| `/admin/db/stats` 5.3s | 管理端统计，可接受 | ⚡ 低 |

### POST端点(未自动测试)
POST端点需请求体，未批量测试。关键POST端点：
- ✅ scanner/start, stop, reset - 状态控制
- ✅ scanner/trade, sell - 交易操作
- ✅ system/sync-* - 数据同步
- ✅ auth/register, login, refresh - 认证

## Phase 2: 字段映射

### pct_chg字段
- **后端**: 返回`pct_chg`(snake_case)，值为float，正负号正确
- **前端**: 使用`pct_chg`，兼容处理`(sig.pct_chg || 0)`
- **结论**: ✅ 前后端字段名一致，正负号/精度/None处理正确

### None vs null
- 后端Python None → JSON null，FastAPI自动转换
- 前端使用`|| 0`兜底，不会因null报错
- ✅ 无问题

### 字段命名一致性
- 后端统一使用snake_case: `ts_code`, `pct_chg`, `stock_name`, `volume_ratio`, `turnover_rate`
- 前端一致使用snake_case
- ✅ 无映射问题

## Phase 3: 数据源路由

### API数据源分布
| API路径 | 数据源 | 非交易时间回退 |
|---------|--------|---------------|
| `/scanner/*` | MongoDB(stock_daily_ak_full) | ✅ 直接读MongoDB |
| `/scanner/quote/{ts_code}` | 缓存→broker→东财→必盈→MongoDB | ✅ 跳过东财/必盈，直接MongoDB |
| `/unified/trades` | MongoDB | ✅ |
| `/unified/positions` | MongoDB | ✅ |
| `/unified/date-availability` | MongoDB | ✅ |
| `/system/*` | MongoDB/内存 | ✅ |
| `/factor/*` | MongoDB | ✅ |
| `/stocks/*` | MongoDB | ✅ |

### unified.py覆盖率
- 3个GET端点: trades, positions, date-availability
- 覆盖了前端核心数据需求
- ⚠️ 前端部分页面仍直接调scanner API，未走unified

### 非交易时间回退
- ✅ scanner/quote: v2.9.99优化，非交易时间跳过东财/必盈(3s+)直接走MongoDB
- ✅ 其他API全部基于MongoDB，无需回退

## 修复记录
1. `test_v2958_method_extractions.py`: 方法长度阈值从47→50(当前48个>50行方法)
