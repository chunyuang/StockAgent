# 后端API审查报告 - 2026-07-11

## Phase 0: Issue队列
- ✅ 待修队列为空

## Phase 1: 全端点测试结果
- **117 GET端点正常** / 22个异常
- 异常分类:
  - 401 (需要认证): `/users/me*`, `/backtest/history` — 正常行为
  - 404 (路由未注册): market_* (19个), report (2个) — **已确认死代码，前端未引用**
  - 422 (缺少参数): `/factor/view` — 需要 ts_code 查询参数，正常行为

### 慢端点 (>2s)
| 端点 | 耗时 | 原因 |
|------|------|------|
| admin/db/stats | 5111ms | MongoDB全集合统计 |
| sys/auto-fill | 4307ms | 检测缺失数据 |
| factor/batch-view | 3540ms | 批量因子查询 |
| sys/data-status | 3108ms | 多数据源状态汇总 |

## Phase 2: 字段映射
- **前后端字段名基本一致** — snake_case统一
- 关键字段名对齐: `pct_chg`, `profit_pct`, `current_price`, `stop_loss_pct`, `take_profit_pct`
- 发现 `trailing_stop_pct` 存在浮点精度问题: `0.060000000000000005` (前端用toFixed处理)
- `pct_chg` 正负号一致: 跌为负，涨为正
- None vs null: FastAPI自动将Python None序列化为JSON null，前端用 `??` 运算符处理

## Phase 3: 数据源路由
| API模块 | 数据源 | 说明 |
|---------|--------|------|
| stock.py | MongoDB (stock_daily_ak_full, stock_basic) | 纯本地读取 |
| unified.py | MongoDB | 3个API全部读MongoDB |
| scanner_core/scan/trading | MongoDB + Scanner内存状态 | 运行时状态+历史 |
| scanner_scan.py quote/ | BiyingAdapter(东财) | 实时行情 |
| system.py sync-* | 东财/AKShare/Tushare | 数据同步脚本 |
| datasource.py | 全源管理 | 切换/测试接口 |

### 非交易时间回退
- unified.py: 纯MongoDB读取，天然支持非交易时间
- stock.py realtime: 读stock_daily_ak_full最新记录，非交易时间返回上一交易日
- scanner_scan.py: 通过BiyingAdapter实时获取，失败回退MongoDB

### unified.py覆盖率
- `/trades`, `/positions`, `/date-availability` — 3个API
- 覆盖前端Monitor页面核心数据需求

## 修复项
1. **Contract test snapshot更新**: 10个API结构变化(scanner status/summary/positions等新增strategy_summary字段)
   - 已运行 `--update-snapshot` 更新

## 死代码(非Bug)
1. **report_router**: report.py定义了路由但从未注册到app.py，前端也未引用
2. **market_router**: app.py中已注释掉("行情分析/板块分析/热点追踪已从前端移除")，前端未引用

## 测试结果
- ✅ pytest: 1883 passed, 0 failed, 8 skipped
- ✅ vite build: 成功 (12.78s)
