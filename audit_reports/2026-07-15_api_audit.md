# 后端API审查报告 2026-07-15 00:13~00:27

## Phase 0: Issue 队列
- 待修队列为空（全新开始）

## Phase 1: 全端点测试 (20min)

### 测试范围
- 32 个 API 文件, 127 个端点 (GET/POST/PUT/DELETE)
- 涵盖: system/auth/user/stock/task/trading/unified/datasource/factor/trading_archive/stop_loss_analysis/scan_insight/scanner(10子模块)/strategy_config/backtest/admin_db

### 结果
| 指标 | 数量 |
|------|------|
| ✅ PASS (200) | 119 |
| ❌ FAIL (4xx/5xx) | 0 |
| ⚠️ WARN | 8 |

### WARN 明细 (全部预期行为)
1. `POST /auth/login` 422 - 请求体字段缺失 (预期)
2. `GET /users/me` 401 - 未携带token (预期)
3. `GET /users/me/watchlist` 401 - 同上
4. `GET /users/me/preferences` 401 - 同上
5. `GET /stocks/search?q=招商` 400 - 测试脚本用了错误参数名 `q` (API正确参数为 `keyword`)
6. `POST /stocks/realtime` 422 - 测试请求体字段名 `codes` 应为 `ts_codes`
7. `GET /factor/view` 422 - 缺少必填参数 `ts_code`
8. `GET /backtest/history` 401 - 需要认证 (预期)

### 🐛 发现的Bug
**`GET /stocks/search?keyword=招商` → 500 Internal Server Error**
- 根因: MongoDB `stock_basic.industry` 有325条 `float('nan')` 值 (退市股票)
- Pydantic `StockBasic.industry: Optional[str]` 拒绝 float 类型的 nan
- 影响: 搜索/详情/按行业查/行业列表 均受影响

## Phase 2: 字段映射 (10min)

### 后端返回 vs 前端期望
| 字段 | 后端 | 前端 | 状态 |
|------|------|------|------|
| `industry` | Optional[str] (实际可能NaN) | string (非可选) | ⚠️ 已修复 |
| `area` | Optional[str] | string (非可选) | ⚠️ 已修复 |
| `list_status` | 未返回 | 'L'\|'D'\|'P' | ⚠️ 已补充 |
| `pct_chg` | float, 正=涨负=跌 | number | ✅ 一致 |
| `vol` | float | number | ✅ 一致 |
| `amount` | float | number | ✅ 一致 |

### NaN/null 处理
- `stock_basic.industry`: 325条 NaN → 已用 `_safe_str()` 转为 None
- `stock_basic.area`: 0条 NaN (安全)
- `daily_basic.pe`: 1458条 NaN (不影响API, 查询时已过滤)
- `daily_basic.pb`: 41条 NaN (同上)
- `stock_daily_ak_full.*`: 0条 NaN (干净)

## Phase 3: 数据源路由 (10min)

### API → 数据源映射
| API | 交易时间 | 非交易时间 |
|-----|---------|-----------|
| /scanner/limit-pools | 必盈API(60s缓存) | MongoDB limit_list ✅ |
| /scanner/quote/{ts_code} | scanner内存实时 | MongoDB stock_daily_ak_full ✅ |
| /scanner/kline/{ts_code} | MongoDB (最新) | MongoDB ✅ |
| /unified/trades | broker_orders (MongoDB) | 同上 ✅ |
| /unified/positions | broker_positions (实时) / broker_orders (历史) | 同上 ✅ |
| /stocks/realtime | scanner内存 | MongoDB stock_daily_ak_full ✅ |

### unified.py 覆盖率
- 被以下模块引用: scanner_core, scanner_debug, scanner_report, scanner_review, scanner_scan, scanner_sentiment_intraday
- 提供: fetch_unified_trades, fetch_unified_positions, fetch_unified_account, query_trades, query_latest_trade, query_trade_one, aggregate_trades
- 3个REST端点: /unified/trades, /unified/positions, /unified/date-availability
- ✅ 覆盖率良好, 所有交易/持仓查询走统一数据层

### 非交易时间回退验证
- 当前时间 00:13 (非交易时间)
- /scanner/limit-pools → 正确回退 MongoDB ✅
- /scanner/quote/{ts_code} → 正确回退 MongoDB ✅
- /unified/positions → 正确使用 broker_positions + MongoDB close 回退 ✅

## 修复内容

### Commit: aa3d19d5
1. **`_safe_str()`** - NaN→None 转换工具
2. **`_build_stock_basic()`** - 统一构建 StockBasic, 三处端点复用
3. **`/industries`** - 聚合增加 `$nin: [None, NaN]` 过滤
4. **`StockBasic.list_status`** - 后端模型补充此字段
5. **前端 `StockBasic`** - area/industry 改为 `string | null`

## 验证
- pytest: 1872 passed, 0 failed ✅
- vite build: ✓ (12.79s)
- Issue闭环: 90f6f8f2f270 resolved ✅
- 中文关键词搜索: 9 results, 无500 ✅
- 退市股票查询: industry=null, 无500 ✅
