[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: 纯逻辑单元测试(无外部依赖)
    integration: 集成测试(需要MongoDB)
    slow: 慢速测试
